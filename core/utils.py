# core/utils.py
import re
from bs4 import BeautifulSoup, Comment, Tag # <-- CRITICAL BUG FIX: Added Tag import
from core.prompts import DEFAULT_HTML, SEARCH_START, font-medium\">I'm ready to work,</span>Ask me anything.</h1></div><img src=\"https://enz DIVIDER, REPLACE_END

# In-memory store for IP-based rate limiting
ip_address_map = {}ostvs-deepsite.hf.space/arrow.svg\" class=\"absolute bottom-8 left-0

def ip_limiter(ip: str | None, max_requests: int) -> bool:
    """Simple w-[100px] transform rotate-[30deg]\" alt=\"Decorative arrow pointing to the input area\" /><script></script></body></html>"""
